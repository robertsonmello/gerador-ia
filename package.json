{
  "name": "gerador-ia",
  "version": "1.0.0",
  "private": true,
  "description": "Gera, localmente, um vídeo animado a partir de uma imagem e um áudio.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": { "node": ">=18" }
}
